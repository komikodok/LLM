{
    "model": {
        "repo_id": "<model> // use this if the model from locally",
        "api_key": "<model_api_key> // use this if the model from api key"
    },
    "mongodb": {
        "db_name": "<db_name>",
        "collection_name": "<collection_name>",
        "collection_history": "<collection_history_name>",
        "index_name": "vector_index",
        "mongodb_connection": "<mongodb_connection>"
    }
}
